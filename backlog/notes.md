**Last session ended 2026-06-19.**

**Where we left off:** Cleaned up CLAUDE.md (Steve: "it's way too big"). Routed the dedup to the documentation-maintainer, caught an over-deletion, committed on a branch, opened PR #35. No backlog tickets were touched — active list is unchanged at 4 (#5, #20, #21, #29).

**Picked up next:** (Steve's call on priority)
1. **Merge PR #35** (`docs/trim-claude-md`) — CLAUDE.md dedup, awaiting review/merge. Non-ticket; just needs the merge.
2. **#29** web-admin: GET /api/sports allows unauthenticated team enumeration (low, pre-existing; decide document-as-public vs wrap `withAuth`). Pairs naturally with **#21** (lock global catalog tables sports/leagues/league_teams to read-only-for-authenticated via RLS) — do them together.
3. **#20** E2E dashboard empty-state via a no-device QA user — **verify-before-working**: an `empty-states.spec.ts` already exists in `web-admin/e2e/`. Check the spec's scope against the ticket before doing any work; could be a quick close.
4. **#5** Automate Pi deploy via Tailscale + GitHub Actions — the YAML/scripting half is agent-doable; only the Tailscale/Pi wiring is host-side. Bigger lift; replaces the manual SSH `git pull`/build/run loop.

**External state to remember:**
- **PR #35 open** (`docs/trim-claude-md`, commit `9b68bba`) — CLAUDE.md dedup, pushed, awaiting merge. Working tree is clean.
- **CLAUDE.md is ~two-thirds template-managed.** Lines 188–600 are the `forge:orchestrator-start/end` marker block, regenerated by `forge upgrade` — DON'T hand-trim it (overwritten next upgrade). Only lines 1–187 are hand-authored. This is why "cleanup" only netted 626→607.
- **web-admin is on Next 15.5.19.** tsc/eslint/build all clean.
- **Supabase migrations through 012** (NBA enable + 30 teams applied to remote, project linked). Next migration is 013.
- **Pi:** SSH alias `led-scoreboard-3` (key auth, aarch64); DHCP IP drifts + `.local` mDNS flaky — re-check on SSH fail. Steve runs `sudo` for hardware runs himself. Matrix build (`-tags matrix`) + on-panel check stay host/Pi.
- **forge `--project` gotcha:** `forge backlog`/`forge invoke` resolve project from cwd — pass `--project /Users/stevebargelt/code/pixtron` or run from repo root (a stray `cd web-admin` breaks them). Also: `forge invoke` may mount `web-admin/` as `/project`, so pass `--project` to let an agent reach top-level files like CLAUDE.md.
- CI: strict `npm ci`; **codecov/patch + codecov/project both gate** — new uncovered branches fail the PR. Forge container skips tsc + prettier (run them explicitly per-brief).

**Decisions worth not relitigating:**
- **CLAUDE.md conventional-commits + no-commit-to-main rules now live ONLY in the `## Guidelines` section**, not the orchestrator block. The first dedup pass deleted them claiming "covered by orchestrator block" — that was FALSE (grep confirmed empty). They were restored. If a future cleanup eyes them again: they survive nowhere else, keep them. (The no-AI-attribution rule IS genuinely in the orchestrator block, so that one was correctly dropped from Guidelines.)
- **Prior session's `.test.ts`-in-API-route-tree non-ticket thread is RESOLVED** — shipped as #34 (commit `2db9782`). Don't re-offer a ticket for it.
- **Prior "uncommitted on purpose" CLAUDE.md/BACKLOG.md thread is RESOLVED** — both committed (BACKLOG via groomings, CLAUDE.md via PR #35). Nothing left riding in the working tree.

**Shipped (for reference):** PR #35 — CLAUDE.md dedup (trailer sections collapsed into `## Guidelines`, dead Python env vars dropped, duplicate no-WebSockets line removed; 626→607 lines). No ticket closures this session.
